

object TestScala {
  def main(args: Array[String]): Unit = {
    maximalSquare(List("0111", "1111", "1111", "1111"))
  }

  def maximalSquare(strArr: List[String]): String = {
    
    List[List[String]] matriz
        var sum = 0

        for 
        
//    for (valor:String <- strArr) {
//      println(valor.length())
//      if(i<valor.length() &&valor(i)=="1"){
//        println("entre 1")
//        println(valor.slice(i, j))
//      }else{
//        println("entre 0")
//        maximalSquare(strArr,i+1)
//      }
//    }      

    strArr(0)
  }

}